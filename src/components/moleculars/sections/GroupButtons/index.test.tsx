import { expectTextToBeInTheDocument } from "config/testUtils/expects";

describe("GroupButtons", () => {
  it("should render without error", () => {

    expectTextToBeInTheDocument("GroupButtons");
  });
});
