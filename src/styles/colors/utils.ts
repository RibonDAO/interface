export const getPrimary = (theme: any) => {
  const { primary } = theme.colors.brand;
  return primary;
}

export const getTertiary = (theme: any) => {
  const { tertiary } = theme.colors.brand;
  return tertiary;
}